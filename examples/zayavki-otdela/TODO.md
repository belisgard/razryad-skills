# TODO: Реестр заявок

Приёмка по `SPEC.md` от 02.09.2026. Главный сценарий прошёл сам: владелец (Игорь), да -
три примера из SPEC.md пройдены вручную в браузере на настоящем URL Google Apps Script.

## Вердикты по спеке

| # | Пункт из SPEC.md | Вердикт | Доказательство |
|---|---|---|---|
| 1 | Открытие страницы без логина/пароля, виден общий список всем четверым | готово | Живой прогон в браузере на реальном URL, подтверждено человеком |
| 2 | Форма «Новая заявка»: поля автор (список из 3), клиент, что нужно, срок, сумма (необязательно) | готово | `app/index.html:193-223`, поля присутствуют и сохраняются (пример 1) |
| 3 | Срок - дата из календаря, формат ДД.ММ.ГГГГ | готово (перепроверено 02.09.2026 после правки) | `app/index.html:216`: `<input type="date" id="input-due">`, конвертация в ДД.ММ.ГГГГ перед сохранением (`isoToRuDate`, `app/index.html:274-278`); подтверждено человеком: календарь открылся, заявка сохранилась, срок в списке в формате ДД.ММ.ГГГГ |
| 4 | Сохранение заполненной формы → заявка в списке со статусом «новая», видна остальным без ручного обновления | готово | Пример 1 пройден вручную, видно во второй вкладке через несколько секунд |
| 5 | Смена статуса из строки (новая/в работе/сделано/отменено) видна остальным | готово | Пример 2 пройден вручную, статус сменился без перезагрузки |
| 6 | Фильтр по статусу показывает только нужный статус | готово | Подтверждено вручную: фильтр «в работе» оставил только такие заявки |
| 7 | Пустое обязательное поле (клиент/что нужно/срок) не сохраняет, подсвечивает | готово | Пример 3 пройден вручную (поле «срок» подсветилось), продублировано на сервере `backend/Код.gs:42-48` |
| 8 | Дубль не блокируется, вручную переводится в «отменено» | готово | Код: `addRequest_` (`backend/Код.gs:42-53`) не проверяет на существующие строки |
| 9 | Нет удаления заявок вообще, только статус «отменено» | готово | Код: в `doGet` (`backend/Код.gs:75-86`) нет операции delete/remove |
| 10 | Сумма необязательна, сохраняется пустой | готово | Код: `sum: p.sum \|\| ''`; отображение «-» при пустом значении принято человеком как ок |
| 11 | Сумма - разделитель тысяч пробел (стандарт `AGENTS.md`: «до рубля, разделитель тысяч пробел») | **не готово**: сумма 100000 сохраняется и отображается без пробела | Подтверждено вручную: ввод «100000» → в списке и в самой Google Таблице «100000» без форматирования |
| 12 | Вне объёма выдержан: нет счётчиков, сортировки, напоминаний, истории переписки, контактов клиента, разделения прав | готово | grep по коду не находит соответствующей логики ни в `app/index.html`, ни в `backend/Код.gs` |
| 13 | Список хранится в общем месте, доступен без компьютера владельца | готово | Google Таблица + Apps Script, подтверждено доступом с реального URL из разных вкладок/режима инкогнито |

## Примеры «вход → верный выход»

| Пример | Вход | Ожидаемый выход | Фактический выход | Вердикт |
|---|---|---|---|---|
| 1 | Аня, Клиент 1, «Замена окна на балконе», 10.09.2026, сумма не указана | Новая строка: Клиент 1 / Замена окна на балконе / 10.09.2026 / сумма пусто / новая / Аня | Заявка сохранилась и появилась в списке со статусом «новая», как ожидалось | готово |
| 2 | Смена статуса заявки Клиент 1 на «в работе» | Статус меняется на «в работе», остальные поля не меняются | Статус сменился без перезагрузки, видно во второй вкладке через несколько секунд | готово |
| 3 | Клиент 2, «Консультация по товару», срок пустой | Заявка не сохраняется, поле «срок» подсвечено | Не сохранилась, поле подсветилось красным | готово |

## Ключ ответов из data/README.md

| Зашитая находка | Должен был найти | Нашёл | Вердикт |
|---|---|---|---|
| Пустая сумма (строки 1, 5, 11-12, 18) | Пусто в столбце «Сумма», ничего не подсвечивать | Код показывает «-» при пустой сумме; подтверждено сохранением примера 1 без суммы | готово |
| Дубль, лишнюю уже отменили вручную (строки 9-10) | Обе строки остаются отдельно, без слияния | Проверено по коду (`backend/Код.gs`, нет логики слияния); полный CSV в живую таблицу не загружался | готово (по коду) |
| Дубль, ещё не прибранный (строки 11-12) | Обе строки видны как отдельные заявки «новая» | Проверено по коду, аналогично предыдущему пункту | готово (по коду) |
| Отменённая заявка остаётся в списке (строки 6, 10, 15) | Статус «отменено» как обычная строка, без способа удалить | Кода удаления нет; полный CSV в живую таблицу не загружался | готово (по коду) |
| Крупная сумма / формат тысяч (строка 17, 100 000) | Сумма с пробелом как разделителем тысяч | Живой тест: сумма сохраняется и отображается без пробела | **не готово** |

Примечание: полный тестовый CSV (`data/test/заявки.csv`, 18 строк) в реальную Google Таблицу не
загружался - три находки выше подтверждены чтением кода, а не прогоном всех 18 строк через живой
интерфейс. Риск низкий: код не содержит специальной обработки дублей/отмены, которая могла бы
повести себя иначе на большем наборе данных, но при желании можно догрузить CSV в таблицу
и провести живую сверку отдельным заходом.

## Сейчас (до выпуска)

- пусто

## Потом (после выпуска)

- [ ] Формат суммы: показывать с пробелом как разделителем тысяч (сейчас «100000», должно быть
      «100 000» по стандарту `AGENTS.md`). Вернуться после выпуска, на работу списка не влияет.

## Никогда (сознательно не делаем)

- пусто
