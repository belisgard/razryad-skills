# Журнал проекта: Реестр заявок

Записи сверху вниз по дате. Старые записи не редактируются, только дополняются новыми.

---

## [02.09.2026] Этап: проект закончен

**Сработало:** смена механики процесса до появления инструмента, менеджеры сами вносят заявки вместо переписки в чате, это убрало главную причину потерь.

**Застрял:** развёртывание Google Apps Script, ~1 час. Причина: неверные настройки доступа («у кого есть доступ»), ошибка «файл не найден».

**Иначе:** сразу ставить доступ «Все» и проверять ссылку в режиме инкогнито перед отправкой, а не после ошибок.

**Правила, добавленные в AGENTS.md:**
- раздел «Термины и стандарты»: «Развёртывание Google Apps Script: доступ сразу ставить «Все», ссылку проверять в режиме инкогнито до того, как отправлять кому-то»

**Набору razryad-skills:** на этапе `razryad-build` не хватало требования самопроверки публичной ссылки в инкогнито до отчёта об успехе; отправлено как issue: нет (в локальной копии набора нет git-репозитория с remote, текст issue подготовлен и показан человеку).

**Второй проект:** не выбран. Решение: понаблюдать за прижившимся инструментом хотя бы пару недель, вернуться с новой задачей позже.

**Осталось в TODO.md:** 1 пункт «потом» (формат суммы: разделитель тысяч пробел, не влияет на работу списка), можно сделать в любой момент до второго проекта.
